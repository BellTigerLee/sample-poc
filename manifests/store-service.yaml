apiVersion: v1
kind: Service
metadata:
  name: sample-poc-ring-store
  labels:
    app.kubernetes.io/name: sample-poc
    app.kubernetes.io/component: ring-store
  annotations:
    lbipam.cilium.io/ips: "10.33.142.20"
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: sample-poc
    app.kubernetes.io/component: ring-store
  ports:
    - name: http
      port: 80
      targetPort: 8080
